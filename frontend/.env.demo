# Static demo build: no backend required.
# Enables the backend-free demo mode where every /api call resolves against the curated
# local catalog in src/services/demoCatalog.js. Used by `npm run build:demo`.
VITE_DEMO_MODE=true
